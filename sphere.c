typedef struct sphere  {
    int[3] center;
    int radius;
}

// returns whether intersection occurs, and if so, writes to a location
int intersection(int[3] start, int[3] directionVector, int[3] intersectionLocation)  {

}